# dev/backend.tf

terraform {
  backend "azurerm" {
    # Apunta al RG creado por el módulo 'tfstate_rg'
    resource_group_name  = "dev-tfstate-rg"
    storage_account_name = "bltex63483"
    container_name       = "tfstate"
    key                  = "terraform.tfstate"

    # No incluir credenciales aquí
  }
}